services:
  - type: web
    name: todoList
    env: node
    buildCommand: npm install && npm run build
    startCommand: npx serve -s dist -l 8080
    buildFilter:
      paths:
        - src/**
    envVars:
      - key: NODE_ENV
        value: production
